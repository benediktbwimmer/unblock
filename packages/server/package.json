{
  "name": "@unblock/server",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "prebuild": "npm run build -w @unblock/core",
    "build": "tsc -p tsconfig.json",
    "precheck": "npm run build -w @unblock/core",
    "check": "tsc -p tsconfig.json --noEmit",
    "pretest": "npm run build -w @unblock/core",
    "test": "vitest run",
    "dev": "tsx src/index.ts"
  },
  "dependencies": {
    "@hono/node-server": "^1.19.6",
    "@unblock/core": "0.1.0",
    "@unblock/prism-app": "0.3.0",
    "hono": "^4.10.5",
    "jose": "^6.2.3"
  }
}
